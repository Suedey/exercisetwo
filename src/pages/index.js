import Data from '../app/Components/data'

export default function Home(){
    const projectName = "Exercise Two"

    console.log(Data);
    return (
        <main>
            <h1>{projectName}</h1>
        </main>
    );
}